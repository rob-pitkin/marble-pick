[project]
name = "marble-pick"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium>=1.2.0",
    "pettingzoo>=1.25.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
